[project]
name = "ravenstack"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.14"
dependencies = [
    "dbt-core>=1.12.0",
    "dbt-duckdb>=1.10.1",
    "elementary-data[duckdb]>=0.25.1",
]
